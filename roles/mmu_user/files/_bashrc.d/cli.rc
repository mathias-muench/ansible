set -o vi

alias h='history'
alias r='fc -e -'
function lv { vim "+lv /$1/ ***"; }

trap 'history -w ~/.local/share/bash_history' EXIT
PROMPT_COMMAND="history -a; $PROMPT_COMMAND"
HISTSIZE=100000
HISTFILESIZE=1000000

export TZ=Europe/Berlin
export TERM=rxvt-256color
