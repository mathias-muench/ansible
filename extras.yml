---
# vi:ai:et:sw=2:ts=2

- hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  module_defaults:
    copy:
      backup: True
  roles:
    - name: extras
